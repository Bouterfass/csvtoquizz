import React from 'react';

interface QuizzProps {
    filename: string;
    language: string;
    level: string;
    length: number;


}

const QuizzCard = ({filename, language, level, length}: QuizzProps) => {

    return <>
        0
    </>

}


export default QuizzCard;