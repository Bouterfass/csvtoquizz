const MedTitle = ({ children }: any) => {

    return (
        <span className='w-full flex justify-center font-bold text-2xl text-lightPurple dark:text-lightGrayL'>
            {children}
        </span>

    )
}

export default MedTitle;