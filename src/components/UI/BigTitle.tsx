const BigTitle = ({ children }: any) => {

    return (
        <div className='py-4 w-full flex justify-center font-bold text-4xl text-lightPurple dark:text-lightGrayL'>
            {children}
        </div>

    )
}

export default BigTitle;